# =====================================================
# 白名单模式：默认忽略所有，仅放行下方显式允许的内容
# =====================================================

# 默认禁止一切文件 / 目录
*

# 允许遍历目录（git 需进入目录才能匹配其中的白名单文件）
!*/

# 以下内容不在白名单内，保持默认忽略：
#   node_modules、dist-ssr、日志文件、*.local、
#   编辑器目录(.vscode/.idea/.DS_Store 等)

# ------- 白名单：根目录文件（以 / 锚定，仅匹配仓库根目录） -------
!/.gitignore
!/.oxlintrc.json
!/LICENSE.md
!/README.md
!/index.html
!/package.json
!/package-lock.json
!/vite.config.js

# ------- 白名单：目录（当前已纳入 git 的全部内容） -------
!/public/**
!/session/**
!/src/**
